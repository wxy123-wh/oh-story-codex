# {项目名} — 事件制网文写作工具集（Codex）

<!-- STORY-SETUP:BEGIN -->

## 核心文件

本项目采用少文件事件制：

- `写作规则.md`：固定写作规则、男频收获循环、台词差异化、正文禁令。
- `全局状态.md`：跨事件、跨卷承接，只记录后续会用到的状态。
- `事件库.md`：规划核心。每卷若干事件，每个事件内部分章，素材写成正文级。
- `正文/`：正文章节。

不默认维护 `设定/`、`大纲/`、`追踪/`、`伏笔/`、`时间线/` 等多文件结构。设定和人物资料优先写进事件现场；状态变化只压缩进 `全局状态.md`。

## Skill 路由表

| 命令/意图 | Skill | 说明 |
|---|---|---|
| `$story-setup`、准备写书 | story-setup | 初始化事件制项目和 Codex hooks/agents |
| `$story-event-plan`、开书/开卷/生成事件 | story-event-plan | 生成或更新 `事件库.md`，事件内分章 |
| `$story-long-draft`、日更/续写 | story-long-draft | 按 `事件库.md` 当前事件写正文 |
| `$story-event-settle`、结算事件 | story-event-settle | 事件完成后更新 `全局状态.md` |
| `$story-review`、审查 | story-review | 检查事件收获、压力、状态、正文质量 |
| `$story-deslop`、去 AI 味 | story-deslop | 清理正文 AI 味和退化问题 |
| `$story-long-analyze`、拆文 | story-long-analyze | 拆解对标作品 |
| `$story-long-scan`、扫榜 | story-long-scan | 扫榜和选题 |
| `$story-import`、导入 | story-import | 导入已有小说 |
| `$story`、网文 | story | 模糊意图路由 |

旧 `$story-long-volume-outline` 和 `$story-long-chapter-outline` 只作为兼容入口，实际应写入 `事件库.md`，不再生成传统大纲/细纲文件。

## 写作流程

1. 先用 `$story-event-plan` 生成本卷 4-8 个事件。
2. 每个事件内部直接分章，每章包含目标、正文级素材、关键对白、章尾钩子。
3. 用 `$story-long-draft` 按事件顺序写正文。
4. 一个事件写完后，用 `$story-event-settle` 更新 `全局状态.md`。
5. 下一事件只读 `写作规则.md`、`全局状态.md`、`事件库.md` 和最近正文继续。

## Codex 项目约定

- Codex custom agents 部署在 `.codex/agents/*.toml`，需要新开 Codex 会话后才会稳定可用。
- Codex hooks 部署在 `.codex/hooks.json` 和 `.codex/hooks/`；项目 `.codex/` 层需要被信任。
- 写正文前必须有 `事件库.md`，且当前事件已内部分章。
- Compact 后优先读取 `全局状态.md` 恢复当前进度。

## Compact 后恢复上下文

恢复顺序：

1. `全局状态.md`
2. `事件库.md` 当前事件
3. `正文/` 最近一章
4. `写作规则.md`

<!-- STORY-SETUP:END -->
